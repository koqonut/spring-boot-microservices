server:
  port: 3006

spring:
  application:
    name: HYSTRIX-DASHBOARD


hystrix:
  dashboard:
    proxy-stream-allow-list: '*'
    
management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    enabled: false