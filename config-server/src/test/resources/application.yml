server:
  port: 3007

spring:
  application:
    name: springboot-microservice-demo
  cloud:
    config:
      server:
        git:
         # uri: /home/kaustav/projects/sts/projects/config-folder
          uri: https://github.com/koqonut/springboot-microservice-demo
          clone-on-start: true
          
eureka:
  client:
    enabled: false